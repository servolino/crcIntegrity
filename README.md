# crcIntegrity
2 modes of operation: calculation mode for CRC-12 value for given input string, and verification mode which will verify the correct CRC-12 value is appended at the end of a value for a given input string.
